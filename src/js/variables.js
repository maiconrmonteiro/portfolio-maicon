const header = document.getElementById("header")
const theme = document.getElementById("theme")
const headerText = document.querySelector(".inner")
var icon = document.querySelectorAll(".fa-x")
const aboutMe = document.querySelectorAll("li")[0]
const projects = document.querySelectorAll("li")[1]
const skills = document.querySelectorAll("li")[2]
const contacts = document.querySelectorAll("li")[3]

export { header, theme, headerText, icon, aboutMe, projects, skills, contacts }